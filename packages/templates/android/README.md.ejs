# <%- appName %>

Android app skeleton generated by **Mobile Initializr**, built on the company mobile framework
(version `<%- frameworkVersion %>`).

- **Package:** `<%- packageName %>`
- **Min SDK:** <%- minSdk %>
- **UI:** Jetpack Compose + Material 3
- **DI:** Hilt

## Selected modules

<% if (modules.length === 0) { -%>
_No optional modules selected._
<% } else { -%>
<% modules.forEach(function (m) { -%>
- **<%- m.name %>** — `<%- m.group %>:<%- m.artifact %>`
<% }); -%>
<% } -%>

## Getting started

```bash
# Generate the Gradle wrapper (one time, if not present):
gradle wrapper --gradle-version 8.10.2

./gradlew assembleDebug
```

> The company modules resolve from the internal Maven repository configured in
> `settings.gradle.kts`. Ensure your credentials for that repo are set up locally.
