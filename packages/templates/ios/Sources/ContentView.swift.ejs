import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Welcome to <%- appName %>")
                .font(.title)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
