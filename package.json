{
  "name": "mobile-initializr",
  "version": "0.1.0",
  "private": true,
  "description": "An internal start.spring.io for native mobile apps (Android + iOS).",
  "packageManager": "pnpm@8.6.7",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter @initializr/web dev",
    "test": "pnpm -r test"
  }
}
